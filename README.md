# AI Test Generator

基于 DeepSeek API 的测试用例自动生成工具。

## 使用

```bash
pip install openai rich
```

设置环境变量：

```bash
set OPENAI_API_KEY=your_api_key
```

运行：

```bash
python ai_test_gen.py
```

输入需求描述，即可自动生成 3 条测试用例。
